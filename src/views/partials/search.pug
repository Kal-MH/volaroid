.search-container
    .search
        h1 Search
        form.search-form(action=routes.search, method="get")
            input(type="text", name="term", placeholder="Awesome")
            input(type="submit", value="Search")